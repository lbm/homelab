apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-lachlan-io-staging
  namespace: traefik
spec:
  secretName: wildcard-lachlan-io-tls-staging
  dnsNames:
    - "*.lachlan.io"
  issuerRef:
    name: letsencrypt-staging
    kind: Issuer
